# Audio Setup

The bundled track is stored at `src/assets/hbd.mp3`. It starts after all five
candles have been blown out and then loops until paused.

To replace it, overwrite that file with audio you created, licensed, or have
permission to redistribute. Keep the filename unchanged, or update the import
in `src/components/BackgroundMusic.jsx`.
